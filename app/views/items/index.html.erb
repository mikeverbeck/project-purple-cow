<p style="color: green"><%= notice %></p>

<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <div class="mt-3">
      <%= link_to item.name, item%>
      <%= render item %>
    </div>
  <% end %>
</div>

<%= link_to "New item", new_item_path %>
